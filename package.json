{
  "name": "denver-events",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && node scripts/copy-artist-pages.js",
    "deploy": "gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "chart.js": "^4.5.1",
    "papaparse": "^5.5.3"
  }
}